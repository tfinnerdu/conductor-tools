flask>=3.0.0
flask-sqlalchemy>=3.1.0
psycopg2-binary>=2.9.9
requests>=2.31.0
jq>=1.6.0
pytest>=8.0.0
pytest-flask>=1.3.0
python-dotenv>=1.0.0
APScheduler>=3.10.4
flask-swagger-ui>=4.11.2

# Optional — only needed for the DOB Repair "Fetch via SQL" source
# (app/dob_sql_source.py). Also requires the system ODBC driver manager:
# "ODBC Driver 17/18 for SQL Server" + unixODBC on Linux hosts. Every other
# tab, including CSV-based DOB Repair, works fine without this installed.
pyodbc>=5.0.0
